#include <iostream>
#include <cstdio>

using namespace std;
int a[10];
int main()
{
	a[0] = 1; a[1] = 1;a[2] = 2;
	int n;
	cin >>n;
	int ans = 0;
	for (int i = 0 ; i < n ; i++ ){
		ans+=a[i]*a[n-i-1]
	}

	return 0;
}
